name: Hold Release
run-name: Hold Release

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: mosaic-development-release
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  get-release:
    name: Prepare
    if: github.repository == 'constbogdan/Wholphin' && github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && github.ref_protected
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      artifact_id: ${{ steps.evidence.outputs.artifact-id }}
      version: ${{ steps.get.outputs.version }}
      release_id: ${{ steps.get.outputs.release_id }}
      apk_url: ${{ steps.get.outputs.apk_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.14'
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: zulu
          java-version: '21'
      - uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
        with:
          packages: 'platform-tools build-tools;36.0.0'
      - name: Resolve current Stable release
        id: get
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 -B scripts/mosaic_hold_release.py get --directory "$RUNNER_TEMP/mosaic-hold"
      - name: Verify APK package, version, signer, and provenance
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 -B scripts/verify_mosaic_apk.py --apk "$RUNNER_TEMP/mosaic-hold/Wholphin-release.apk" --provenance "$RUNNER_TEMP/mosaic-hold/provenance.json" --apksigner "$ANDROID_HOME/build-tools/36.0.0/apksigner" --aapt "$ANDROID_HOME/build-tools/36.0.0/aapt" --output "$RUNNER_TEMP/mosaic-hold/verification.json"
          python3 -B scripts/mosaic_hold_release.py verify --directory "$RUNNER_TEMP/mosaic-hold"
      - name: Show hold approval request
        env:
          VERSION: ${{ steps.get.outputs.version }}
          APK_URL: ${{ steps.get.outputs.apk_url }}
        run: |
          printf '## Ready to hold: [%s](%s)\n\nWaiting for approval — authorization is required before changing the current Stable release.\n\nNothing has been changed by this run.\n' "$VERSION" "$APK_URL" >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        id: evidence
        with:
          name: hold-release-${{ steps.get.outputs.release_id }}-run-${{ github.run_id }}-attempt-${{ github.run_attempt }}
          path: ${{ runner.temp }}/mosaic-hold/
          retention-days: 7
          compression-level: 0
          if-no-files-found: error
      - name: Summarize failed preparation
        if: failure()
        run: |
          printf "## Hold Release preparation failed\n\nNothing was changed. Inspect the failed step, correct the problem, and retry.\n\n<details>\n<summary>Technical details</summary>\n\nJob: <code>%s</code>\n\n</details>\n" "$GITHUB_JOB" >> "$GITHUB_STEP_SUMMARY"

  hold:
    name: Hold
    needs: get-release
    if: github.repository == 'constbogdan/Wholphin' && github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && github.ref_protected
    permissions:
      contents: write
      actions: read
    environment: release-hold
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.14'
      - name: Require exact authenticated evidence artifact
        env:
          ARTIFACT_ID: ${{ needs.get-release.outputs.artifact_id }}
        run: '[[ "$ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]'
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          artifact-ids: ${{ needs.get-release.outputs.artifact_id }}
          digest-mismatch: error
          merge-multiple: true
          path: ${{ runner.temp }}/mosaic-hold
      - name: Hold current Stable release
        id: hold
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 -B scripts/mosaic_hold_release.py hold --directory "$RUNNER_TEMP/mosaic-hold"
      - name: Show hold result
        env:
          HELD: ${{ steps.hold.outputs.held }}
          HELD_URL: ${{ steps.hold.outputs.held_url }}
          CURRENT: ${{ steps.hold.outputs.current }}
          CURRENT_URL: ${{ steps.hold.outputs.current_url }}
        run: |
          if [[ "$CURRENT" == 'none' ]]; then
            printf '## Held: [%s](%s)\n\nThis release is no longer advertised as Stable.\n\nCurrent Stable: none available.\n' "$HELD" "$HELD_URL" >> "$GITHUB_STEP_SUMMARY"
          else
            printf '## Held: [%s](%s)\n\nThis release is no longer advertised as Stable.\n\nCurrent Stable: [%s](%s)\n' "$HELD" "$HELD_URL" "$CURRENT" "$CURRENT_URL" >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Summarize failed hold
        if: failure()
        run: |
          printf "## Hold Release refused or failed\n\nHold completion is not confirmed. Inspect the failed step and current Stable state before retrying.\n\n<details>\n<summary>Technical details</summary>\n\nJob: <code>%s</code>\n\n</details>\n" "$GITHUB_JOB" >> "$GITHUB_STEP_SUMMARY"
